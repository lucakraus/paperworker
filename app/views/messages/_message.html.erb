<div id ="message-<%= message.id %>">
  <small>
    <strong><%= message.user.name %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
  <% if message.photo.attached?%>
    <%= cl_image_tag message.photo.key, height: 200, width: 100, crop: :fill %>
  <% end %>
  </div>
